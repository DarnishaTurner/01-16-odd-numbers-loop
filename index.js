let i = 1;
const endNum = 10; // Change this number to test with different ranges

// TODO: Use a do-while loop to iterate and log odd numbers within the range

do {
    if(i % 2 !== 0) {
        console.log(i);
    }
    i++;
} while (i <= endNum);